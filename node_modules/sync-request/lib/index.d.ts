/// <reference types="node" />
import { HttpVerb, Response } from 'sync-request/lib/node_modules/then-request';
import { URL } from 'url';
import { FormData } from 'sync-request/lib/FormData';
import { Options } from 'sync-request/lib/Options';
export { HttpVerb, Response, Options };
export { FormData };
export default function request(method: HttpVerb, url: string | URL, options?: Options): Response;
